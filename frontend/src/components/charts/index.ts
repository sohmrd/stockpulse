/**
 * Charts component barrel export.
 *
 * Exports are added here as chart components are implemented:
 *   - StockChart     (Sprint 2) — Recharts line/candlestick chart for stock prices
 *   - PortfolioPie   (Sprint 3) — Recharts pie chart for portfolio allocation
 *   - PerformanceLine (Sprint 3) — Recharts line chart for P&L over time
 *   - MiniSparkline  (Sprint 2) — Inline sparkline for watchlist rows
 */

export { default as PriceChart } from "./PriceChart";
